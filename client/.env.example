# ShareBite AI — client environment (Vite)
# Local dev + single-server prod need NOTHING (same-origin /api + socket).
# Only set this for SPLIT hosting (frontend on Vercel/Netlify, backend elsewhere):
# VITE_SOCKET_URL=https://your-backend-domain
